# PCスリープ/レジューム時のフリーズ調査

Found (2026-07-29): ThinkPad X1 Nano Gen 1 (BIOS N2TET92W 1.70, kernel 7.0.0-28-generic, ACPI reports full S3 support) にて、gnome-shellのhash-tableクラッシュとは*別の*障害モードを確認: サスペンド中またはレジューム直後にマシン全体がハードフリーズし、journaldにもそれ以降何も記録されない（クリーンシャットダウンなし、assertionなし、完全な沈黙）。ハード電源断が必要となり、journalctlのboot IDが連続した1つのブートではなく増分する形で現れる。

2026-07-29の5回のブートにわたるjournalctl調査で、両方のフリーズ事象がSynaptics WBDI指紋センサー（USB ID 06cb:00bd, "Synaptics Sensors"）と相関することを確認:

- **15:03のフリーズ**: サスペンド要求の24秒前にセンサーがUSBの "No such device" 非同期エラーを投げ、その後サスペンド開始と同時にfprintdが "device is still busy" とログ出力。カーネルログはその後 `PM: suspend entry (deep)` の1行で完全に止まる（通常の "Filesystems sync" 行にすら到達していない）— これはデバイスのサスペンドコールバックがdpm_suspend内でハングした典型的な兆候。
- **16:58のフリーズ**: カーネルレベルのサスペンド/レジュームは約1秒で正常に完了（`PM: suspend exit` に到達、wifiも再接続）したが、レジューム後に指紋センサーが新しいUSBデバイスとして再列挙されてから約11秒後にログが完全に沈黙し、マシンが停止。
- 同日08:51には、fwupdとfprintdが同じデバイスを取り合っている様子も確認（"device is already open" / "already claimed"）— 2つのデーモンがセンサーをプローブ/占有しようとして競合。

**背景**: ユーザーによると、スリープ/レジューム失敗は最近は稀だったが2026-07-29に再び頻発（1日に5回のブート、うち2回がハードフリーズ）。これはgnome-shellのクラッシュ調査とは別問題（あちらはGNOME Shellのみのクラッシュでログイン画面に戻り、journalは生きたまま`g_hash_table_iter_next`のassertionが出る；こちらはjournalが完全に沈黙する全システムフリーズで、GNOME拡張機能とは無関係）。

また、`libfprint-2-2` / `libfprint-2-tod1` に未適用のアップデート（24.04.7 → 24.04.8）があることも確認済み（2026-07-29時点）。

## 試したこと（結果）

1. **libfprintのアップデートを適用**（24.04.7 → 24.04.8）→ 適用済み確認
2. **指紋認証を無効化して切り分けテスト**（`systemctl mask fprintd.service`）
   → 2026-07-30 10:26頃に実施、fprintdは確実にマスクされ動作していなかったが、それでも同一シグネチャでフリーズ再発。**fprintd/libfprintデーモン自体は原因ではないと結論。**
3. **指紋センサーのUSBポートを論理的に無効化してテスト**（`echo 0 | sudo tee /sys/bus/usb/devices/3-3/authorized`）
   → 2026-07-30 10:33頃に実施したが、書き込み後もデバイスが3回再列挙され直しており、実際には切断状態を維持できていなかった（10:38に再度フリーズ、シグネチャは同一）。**このテスト方法自体が不完全だった。**

## 新しい手がかり

上記テスト中のログに、フリーズ直前に必ず次の行が出現していることを確認:
```
xhci_hcd 0000:00:0d.0: xHC error in resume, USBSTS 0x401(または0x411), Reinit
```
`0000:00:0d.0` はIntel Tiger Lake-LP Thunderbolt 4 USBコントローラ。ただし過去5週間分のログを遡ると、このエラー自体は1日に数回、合計85回も出現しており、そのほとんどはフリーズを伴わず自己復旧している。つまり「このエラーが出る」＝「フリーズする」ではなく、**明示的なサスペンド要求がこのコントローラの自己再初期化サイクルとタイミング的に重なった時だけフリーズに至っている可能性**が高い。指紋センサー個体の問題という説は棄却し、Thunderbolt4 xHCIコントローラのランタイムPM絡みの競合を本命の疑いとする。

## 次に試すこと

1. `usbcore.autosuspend=-1`（カーネルパラメータ）でUSBオートサスペンド自体を無効化し、xHC reinitの頻度とフリーズの両方が止まるか確認 → 2026-07-30 14:35から適用中（boot `ef8cceac`〜）。フリーズは止まっていないが、輝度低下という新パターンが出現。
2. BIOS/Thunderboltファームウェアのアップデート確認（`USBSTS 0x401/0x411` resume errorはTiger Lake TBT4コントローラで既知の不具合系統の可能性）
3. まだ試していない: `mem_sleep_default=s2idle` に切り替えてdeep sleep特有の問題か確認

## 新しい症状: レジューム後にメイン(内蔵)パネルの輝度が異常に低くなる

2026-07-30 15:46頃のレジュームで発生。ログアウトしても直らず（GDMログイン画面用の新しいコンポジタが起動した後も暗いまま）、コンソールから`systemctl reboot`相当の明示的な再起動をかけてようやく復帰した。journalctl調査の結果:

- ログアウト自体はクリーンに完了（Xサーバー正常終了、GDMの新セッションが正常起動）→ GNOME Shell/Mutter固有のバグではない
- 該当時間帯、i915/drm/psr/backlight/panel/edp関連のカーネルエラーは**一切なし** → ドライバは「正常にパネルを駆動できている」と認識したまま
- `intel_backlight`のsysfs輝度値も変化なし → PWMバックライトのハード故障ではない
- 再起動はクリーンな要求によるもの（ハングやハード電源断ではない）

**見立て**: eDPパネルのPSR(Panel Self Refresh)やDP-AUXリンクの状態が、レジューム時にドライバの認識と物理パネルの実際の状態がズレたまま固まった可能性が高い。ドライバがエラーを検出しないためログにも残らず、GPU/カーネルの完全リセット（再起動）でしか直らない。

## 重要な発見: カーネルバージョンの切り替わりと発症時期が一致

`journalctl --list-boots` と `/var/log/dpkg.log` を確認したところ:

- 2026-07-14〜07-29 09:22の約15日間は **カーネル 6.17.0-40-generic** で稼働しており、この間フリーズも輝度低下も一切発生していない
- カーネル 7.0.0-28-generic（HWE 7.0系）自体は07-22にインストール済みだったが、実際に起動したのは**07-29 09:22の再起動が初めて**
- **その直後から**（07-29 15:03の初回ハードフリーズ以降、07-30の輝度低下含め）今回調査してきた不具合がすべて発生開始。それ以降は一貫して7.0.0-28で稼働中

→ フリーズも輝度低下も、**カーネル6.17→7.0への切り替わりによるリグレッション**である可能性が高い。7月29日夜のapt upgrade（libc6, fprint等）はこれらの初発生より後なので無関係。

## 次に試すこと（優先・未着手 — 明日以降）

1. **GRUBの "Advanced options for Ubuntu" から `6.17.0-40-generic` を明示的に選んで起動し、しばらく通常運用してみる。** フリーズ・輝度低下のどちらも再発しなければ、7.0系カーネルのリグレッションでほぼ確定。これが最優先の切り分けテスト。
2. `usbcore.autosuspend=-1` は継続中だが今のところ輝度低下は防げていない（フリーズについても未確定）
3. 輝度低下が再発した場合、再起動前に `sudo cat /sys/kernel/debug/dri/1/eDP-1/psr_status`（カード番号は要確認）でPSR状態を直接確認できると仮説の裏付けになる
